<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Arimo:ital,wght@0,400..700;1,400..700&family=Open+Sans:ital,wght@0,300..800;1,300..800&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Quicksand:wght@300..700&display=swap" rel="stylesheet">
<style type="text/css">

  body {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    height: 100vh;
    margin: 0;
    background-image: url('https://indieground.net/wp-content/uploads/2023/03/Freebie-GradientTextures-Preview-02.jpg');
    background-size: cover;
    background-repeat: no-repeat;
    font-family: 'Poppins';
  }

  nav {
    width: 100%;
    background-color: #333;
    color: white;
    padding: 10px 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .nav-title {
    font-size: 24px;
    margin-left: 100px;
  }

  .nav-links {
    font-size: 18px;
    margin-right: 100px;
  }

  nav a {
    text-decoration: none;  /* Removes underline */
    font-family: inherit;  /* Ensures links use the default font */
    color: white; /* Explicitly set the color again if needed */
    padding: 0 10px;
  }

  .crud-form {
    border: 2px solid #f1f1f1;
    padding: 16px;
    background-color: white;
    width: 30%;
    box-sizing: border-box;
    margin-top: 20px;
  }

  input {
    width: 90%;
    padding: 15px;
    margin: 5px 0 22px 0;
    display: inline-block;
    border: none;
    background: #f1f1f1;
    font-family: 'Poppins';
  }

  input:focus {
    background-color: #ddd;
    outline: none;
  }

  button {
    background-color: #434140;
    color: white;
    padding: 10px 20px;
    margin: 8px 0;
    border: none;
    cursor: pointer;
    width: 100%;
    font-size: 20px;
    font-family: 'Poppins', sans-serif; /* Ensure Poppins font is used and has a fallback */
    opacity: 0.9;
    transition: background-color 0.3s ease, color 0.3s ease, transform 0.2s ease; /* Smooth transition for hover effects */
  }
  
  button:hover {
    background-color: #c0c0c0;
    color: #424242; /* Slightly lighter color for text on hover */
    transform: translateY(-2px); /* Subtle lift effect */
    box-shadow: 0 4px 8px rgba(0,0,0,0.2); /* Soft shadow for depth */
  }
  
</style>

</head>
<body>
<nav>
  <div class="nav-title">Final Project</div>
  <div class="nav-links">
    <a href="/crud/form" style="color: white; padding: 0 10px;">Create Data</a>
    <a href="/crud/read" style="color: white; padding: 0 10px;">View Data</a>
  </div>
</nav>
<div class="crud-form">
    <form action="<%= (typeof editData !== 'undefined') ? '/crud/update/' + editData.id : '/crud/create' %>" method="POST">
      <input type="text" placeholder="Full Name" name="full_name" required value="<%= (typeof editData !== 'undefined') ? editData.full_name : '' %>">
      <input type="email" placeholder="Email Address" name="email_address" required value="<%= (typeof editData !== 'undefined') ? editData.email_address : '' %>">
      <input type="text" placeholder="City" name="city" required value="<%= (typeof editData !== 'undefined') ? editData.city : '' %>">
      <input type="text" placeholder="Country" name="country" required value="<%= (typeof editData !== 'undefined') ? editData.country : '' %>">
      <button type="submit">Submit</button>
    </form>
</div>
</body>
</html>
